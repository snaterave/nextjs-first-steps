import type { Metadata } from "next";

export const metadata: Metadata = {
  title: "SEO Contact",
  description: "SEO Contact",
  keywords: ["contact page", "Silma natera", "información"],
};

export default function ContactPage() {
  return <span>ContactPage</span>;
}
